Stop a managed local background process by `processId`.

Use this when a dev server, watcher, or other long-running command should be terminated.

This is primarily a recovery and lifecycle tool for managed background processes.

Notes:
- `processId` is required
- `force` sends a stronger termination signal when a normal stop is not enough
