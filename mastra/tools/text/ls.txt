Lists files and directories in a given path. Paths are resolved under /workspace; omit it to use the current workspace directory (/workspace, Vite starter project). You can optionally provide an array of ignore patterns (regex or plain text) with the ignore parameter. You should generally prefer the Glob and Grep tools, if you know which directories to search.
